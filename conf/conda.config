// Container configuration
// Container runtime is configured via profiles in conf/profiles.config
// Default container image for all processes
process {
  container = 'ghcr.io/shians/lupus-workflow-container:latest'
}
